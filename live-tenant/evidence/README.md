# Live Workflow Evidence

The GitHub Actions workflow writes verified before/after evidence here after it
successfully executes the Keycloak mover workflow.

Expected generated files:

- `keycloak_mover_evidence.json`
- `keycloak_change_record.md`

Do not add passwords, access tokens, raw container secrets, or unrelated identity data.
